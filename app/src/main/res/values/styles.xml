<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorBadass</item>
    </style>

    <style name="PurpleText" parent="AppTheme">
        <item name="android:textColor">#bf00ff</item>
    </style>

    <style name="SmartBg" parent="AppTheme">
        <item name="android:itemBackground">@color/colorSmart</item>
    </style>

</resources>
